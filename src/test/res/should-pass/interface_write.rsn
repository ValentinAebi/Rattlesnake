
struct A : I {
    var x: Int,
    y: Double
}

struct B : I {
    var x: Int
}

interface I {
   var x: Int
}

package Test {

    fn mulX(z: I, factor: Int){
        z.x *= factor;
    }
    
    fn testF() -> Int {
        val a = new A(25, 12.5);
        var t = a.x;
        me.mulX(a, 32);
        t -= a.x;
        return t;
    }

}
