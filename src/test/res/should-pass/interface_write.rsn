
struct A : I {
    var x: Int,
    y: Double
}

struct B : I {
    var x: Int
}

interface I {
   var x: Int
}

package Test {

    fn mulX(r: Region^, z: I^{r}, factor: Int){
        z.x *= factor;
    }
    
    fn testF() -> Int {
        val s = newregion;
        val a = new@s A(25, 12.5);
        var t = a.x;
        me.mulX(s, a, 32);
        t -= a.x;
        return t;
    }

}
